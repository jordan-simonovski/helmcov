suite: mode-config
templates:
  - templates/mode-config.yaml
tests:
  - it: renders dev mode by default
    asserts:
      - equal:
          path: data.mode
          value: dev
      - equal:
          path: data.policy
          value: mode=dev
      - equal:
          path: data.note
          value: development mode
